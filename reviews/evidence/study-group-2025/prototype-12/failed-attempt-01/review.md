# Prototype-12 Failed Attempt 01

Status: **BLOCKED**

The first ASR attempt produced only 896 segments and 231 segments containing
long repeated-character runs such as `時時時...`. It is retained as negative
evidence and must not be used as the candidate transcript. A guarded rerun is
stored at the prototype root and has `repeatedCharacterSegments=0`.
