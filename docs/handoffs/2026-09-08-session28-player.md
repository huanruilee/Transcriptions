# 第 28 堂播放器修正與音文同步接手紀錄

更新：2026-09-08（Asia/Taipei）

## 使用者需求與狀態

- 正式頁面：https://huanruilee.github.io/Transcriptions/?course=shi-liang-lun-er#session-28
- 希望影片可浮動置頂；回報聲音與文字不同步。
- 使用者最新指定核對範圍：**39:50 前後（2390 秒）**。
- 此範圍尚未實際聽音核對；前一回覆僅確認收到時間點，並未完成音訊比對或修改時間標記。
- 本次使用者授權：將目前成果更新到 GitHub，供其他 Agent 接手。此分支為進行中成果，未合併、未部署。

## 已完成

- `src/App.vue` 新增「浮動置頂／還原影片」按鈕，使用同一 iframe 切換右上角固定顯示，保留播放進度。
- 講末倒數卡片改為接到媒體結束事件才出現；原版在載入時就啟動十秒倒數，曾於正式頁面觀察到未播放便從 28 換到 29。
- 跳播、載入講次時清除結束狀態；YouTube 訊息驗證來源 iframe 與 origin。
- `src/stores/player.ts` 在換講時重設時間與高亮，倒帶到第一句之前時清除殘留高亮。
- `tests/unit_v2/playbackRegression.test.ts` 新增回歸測試，修改前兩項失敗，修改後通過；包含浮動切換保留 iframe、結束事件與非來源訊息檢查。

## 驗證與限制

- `npx vitest run`：21 個測試檔、66 項通過。
- `npm run build:v2`：通過。
- `git diff --check`：通過。
- 測試環境有 YouTube iframe 外部 DNS 載入警告；測試通過不代表真實影音對齊驗收。
- 本機 Chrome 實測：浮動影片捲動時仍在右上角；還原／浮動切換後播放繼續。
- 可見位置點擊「昨天在上課時我們提到……」跳到約 1:40，之後 1:54 的高亮為該句。先前語意定位 click 未可靠觸發，改用畫面可見位置點擊才確認；不要將此當作全課音文核對。
- 第 28 堂 487 句，起始時間無逆序、句子無重疊；這只是結構檢查。
- 影片約 85:29，逐字稿終點 85:16.41；長度接近並不能證明中段對齊。
- 未執行舊版 `npm test` 全套；未修改逐字稿、音源、科判或時間標記。

## 下一位 Agent 的工作

1. 取得本分支，確認工作區狀態並讀取本紀錄。
2. 核對 `courses/釋量論第二品/sessions/session_28.json` 中 2390 秒前後句子，對照 YouTube `yzSUab52m-8` 的實際語音。建議至少核對前、中、後三個錨點，區分固定偏移、局部錯位或持續漂移。
3. 記錄實際聽到的內容與影片時間，再決定是否修正逐句時間。不可只以總長度等比例拉伸或猜測秒差。
4. 修改前建立能重現問題的測試；保留原始文字與時間來源，記錄修正證據。
5. 另行檢查播放器切換講次、播放結束與手機浮動視窗行為。本次浮動為頁面內固定視窗，不是跨應用程式的系統置頂視窗。
6. 完成獨立核對後再提出合併／部署，不能把這次 WIP 推送視為音文同步問題已解決。

## 本機預覽

`npm run dev:v2 -- --host 127.0.0.1`

開啟：http://127.0.0.1:3000/?course=shi-liang-lun-er#session-28

預覽服務是否仍在執行需重新確認。
